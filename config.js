window.HELP_SCHOOL_CONFIG = Object.freeze({
  // Si publicas toda la carpeta en Vercel, déjalo como "/api".
  // Si la app permanece en GitHub Pages y NOVA está en Vercel,
  // coloca la URL completa, por ejemplo:
  // NOVA_API_BASE: "https://help-school.vercel.app/api"
  NOVA_API_BASE: "/api",
  SCHOOL_NAME: "HELP SCHOOL",
  LOCALE: "es-PE"
});
